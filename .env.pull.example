# R2 credentials for mirroring production media into local MinIO.
# Copy to .env.pull and fill in your values (see infra/README.md §1 for where to find them).
# Never commit .env.pull — it is gitignored.

R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
